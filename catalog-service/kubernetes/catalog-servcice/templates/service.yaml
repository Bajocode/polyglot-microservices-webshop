apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalog-servcice.fullname" . }}
  labels:
    {{- include "catalog-servcice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalog-servcice.selectorLabels" . | nindent 4 }}
