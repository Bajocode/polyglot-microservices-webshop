apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catalog-servcice.fullname" . }}-test-connection"
  labels:
    {{- include "catalog-servcice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catalog-servcice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
